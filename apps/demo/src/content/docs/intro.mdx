<h2 id="intro">简介</h2>

TipKit 是基于 Tiptap v3 的无头富文本编辑器套件。所谓「无头」，指的是编辑器的逻辑与视觉彻底解耦：核心包只负责状态与命令，所有样式都由主题包（CSS）提供。因此同一套编辑器代码，换一个 CSS 就是另一种风格。

- 技术栈：Tiptap v3 + React 19 + TypeScript strict
- 聚合入口：`@tipkit/editor` 一个组件搞定
- 内置主题：default（shadcn 标准）、sketch（手绘）
- 扩展自包含：目录可整体裁剪，按需引入
