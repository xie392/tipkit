<h2 id="install">安装</h2>

推荐使用 pnpm（monorepo 友好），npm / yarn 亦可：

```bash
pnpm add @tipkit/editor @tipkit/extensions @tipkit/ui @tipkit/components @tipkit/themes
```

其中 `@tipkit/editor` 是聚合入口，其余包（extensions / ui / components / themes）按需引入。想先跑起来，也可以直接克隆仓库，在 `apps/demo` 中查看完整示例。
